Para iterear vamops a usar map


Esto se incluye en el elmeento parde 
{data.map((guitar) => {
    return (
        <>Aqui ira la información que queremos iterar como objetos o api/ tambien si especificamos un elemento este sera actualizado en rfuncion a la db con los props</>

    )
})}

Sintaxis de prop en la funcion debemos indicar el objeto parámetro props 
Puedes acceder a los demas mediante props.price

<Header 
    nombreDelProp = { datos / state o Funciones}
    price={},
    name = useState

    />